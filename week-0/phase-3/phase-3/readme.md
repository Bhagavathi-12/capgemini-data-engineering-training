## Phase 3 – ETL Pipeline

### What I Did

Built an ETL pipeline using PySpark by reading data, cleaning it, performing joins and aggregations, and generating final outputs.

### Methods Used

read(), show(), printSchema(), dropna(), filter(), join(), groupBy(), agg(), sum(), count(), row_number(), write()

### What I Learned

Learned end-to-end ETL and how to apply transformations and window functions to process real-world data.

### Files

pyspark_codes.py – PySpark code
outputs/ – Query results